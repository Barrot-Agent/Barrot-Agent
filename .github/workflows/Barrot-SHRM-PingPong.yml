name: Barrot-SHRM Ping-Pong

on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"  # Run every 15 minutes

permissions:
  contents: write

jobs:
  barrot-ping:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Barrot Ping
        run: |
          TIMESTAMP=$(date -u +"%a %b %e %H:%M:%S UTC %Y")
          mkdir -p memory-bundles SHRM-System
          echo "🏓 Barrot PING sent to SHRM at $TIMESTAMP" >> memory-bundles/outcome-relay.md
          echo "🔵 Barrot PING -> SHRM at $TIMESTAMP" >> SHRM-System/shrm-response-log.md
          
      - name: SHRM Pong Response
        run: |
          TIMESTAMP=$(date -u +"%a %b %e %H:%M:%S UTC %Y")
          echo "🏓 SHRM PONG received from Barrot at $TIMESTAMP" >> SHRM-System/shrm-response-log.md
          echo "🟢 SHRM PONG <- Barrot at $TIMESTAMP" >> memory-bundles/outcome-relay.md
          
          # Update SHRM health status if config exists
          if [ -f "SHRM-System/shrm-config.yaml" ]; then
            sed -i "s/last_pong: .*/last_pong: $TIMESTAMP/" SHRM-System/shrm-config.yaml
          fi
          
      - name: Update Build Ledger
        run: |
          TIMESTAMP=$(date -u +"%a %b %e %H:%M:%S UTC %Y")
          mkdir -p memory-bundles
          echo "{\"ledger\": \"Ping-Pong at $TIMESTAMP\"}" > memory-bundles/build-ledger.json

      - name: Commit Changes
        run: |
          TIMESTAMP=$(date -u +"%a %b %e %H:%M:%S UTC %Y")
          git config user.name "Barrot-Agent"
          git config user.email "barrot@systems.local"
          git add memory-bundles/ SHRM-System/ 2>/dev/null || true
          if ! git diff --staged --quiet; then
            git commit -m "🏓 Barrot-SHRM Ping-Pong cycle executed at $TIMESTAMP"
            git push
          else
            echo "✅ No changes to commit"
          fi
